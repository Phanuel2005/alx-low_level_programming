#include "main.h"

/**
 *_islower - checks to see if there is a lowercase in a group of sentence
 *Return: it returns 1 or 0 which varies with the condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
