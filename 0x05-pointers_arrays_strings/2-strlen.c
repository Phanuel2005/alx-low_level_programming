#include "main.h"

/**
* _strlen -> function to get the length of a string
* @s: string pointer to be passed to this fuction
* Return: returns length of the string
*/
int _strlen(char *s)
{
	return (strlen(s));
	/**
	 * int len;
	 * for (; *s != '\0'; s++)
	 * {
	 * 	len += 1;
	 * }
	 * return (len);
	 */
}
